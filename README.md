# empty-templates

Empty Jahia template set, intended for testing purposes. Contains no components or styles — just the bare minimum to deploy a functional template set on a Jahia 8.2 instance.

## Dependencies

- `default`
